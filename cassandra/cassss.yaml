apiVersion: apps/v1
kind: Deployment
metadata:
  name: cassandra-load-keyspace
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cassandra-load-keyspace
  template:
    metadata:
      labels: 
        app: cassandra-load-keyspace
    spec:
      containers:
      - name: cassandra-load-keyspace
        image: cassandra:5.0
        command:
        - "/bin/bash"
        - "-c"
        - "echo loading cassandra keyspace && ls /data && cqlsh -f /data/schema.cql"
        env:
        - name: CASSANDRA_CLUSTER_NAME
          value: "SDTD Cluster"
        volumeMounts:
        - mountPath: /data
          name: volume
      volumes:
      - name: volume
        hostPath:
          path: /data
