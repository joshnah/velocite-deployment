apiVersion: v1
kind: PersistentVolume
metadata:
  name: cassandra-pv
  namespace: database
spec:
  capacity:
    storage: 500Mi
  accessModes:
    - ReadWriteOnce
  gcePersistentDisk:
    pdName: cassandra-disk
    fsType: ext4
